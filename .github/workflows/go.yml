name: Go

on:
  push:
    branches: [ "main", "dev" ]

jobs:

  build:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v3

    # - name: Set up Go
    #   uses: actions/setup-go@v3
    #   with:
    #     go-version: 1.18

    - name: Setup Environment
      run: docker build -t ubuntu_go . --no-cache

    - name: Run Docker
      run: docker run -d -P ubuntu_go

    # - name: Build
    #   run: go build -v ./

    # - name: Test
    #   run: go test -v ./
